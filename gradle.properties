# Gradle Properties
org.gradle.jvmargs = -Xmx1G
org.gradle.parallel = true

# Mod Properties
version = 0.1
maven_group = net.grandteodor
archives_base_name = grandmod

# Dependencies are managed at gradle/libs.versions.toml
